Attribute VB_Name = "Globals"
Option Explicit

Public Moving As Long, XStart, YStart, LIndex As Long
Public CntObj As Long, FirstPoint As Integer
Public WaitObject As Boolean, CurrentEntity As Integer


Public Sub CountLinks()
'  Dim i As Long, j As Long, cc As Integer
'  For i = 0 To CntLinks - 1
'    cc = 1
'    For j = 0 To i - 1
'      If Links(j).ObjFrom = Links(i).ObjFrom And Links(j).ObjTo = Links(i).ObjTo Then
'        cc = cc + 1
'      End If
'    Next
'    Links(i).LCount = cc
'  Next
End Sub

